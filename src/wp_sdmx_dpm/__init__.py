"""Bidirectional conversion between DPM 2.0 Refit and SDMX 3.1.

See ``docs/transformation-guidelines/`` for the authoritative mapping spec and
the approved plan for the layered architecture (glossary -> data definition ->
constraints -> reporting taxonomy).
"""

__version__ = "0.1.0"
